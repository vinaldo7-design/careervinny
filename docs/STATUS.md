# CareerVinny — STATUS (build stack)

Topmost `next` is the current scope. A scope is done when it PROVES its claim.

## next — ingest v0: one hand-fed role → jd.md
Claim (the test): a single real role is fetched, cleaned, and stored as
`state/roles/{key}/jd.md` with correct frontmatter — and nothing re-fetches it after.
(decisions Q1 · D014)

## later
- **discovery v0** — close the Workday/Taleo blind spot, FREE: poll Workday's public
  `cxs` JSON + a *calibration-grown* company registry (start with a tiny seed, grow it
  from real verdicts). Pair with JobSpy for the public job boards.
- **score-fit v0** — jd.md + rubric → score.md (reuse embedding mechanics; keep the
  bespoke rubric — that's the moat).
- **tailor-cv v0** — master-profile.md + jd.md → tailored CV draft (ResumeLM-style).
- **network v0** — draft outreach into the role folder (bespoke; human reviews + sends).
- **dashboard** — render from score.md.

## done
- (none yet)
